package service;

import model.AuthData;
import model.UserData;
import spark.Request;

public class LogoutService {
    public LogoutService(Request request) {
        AuthData user;
        try{

        }
    }
}
